# Hextech 클라이언트 확장 UI

LoL 클라이언트를 가운데 놓고 왼쪽에서 아군 시너지, 오른쪽에서 상대 상성,
아래에서 추천 후보를 확인합니다. Helper는 중앙을 차지하는 창을 생성하지 않습니다.

## 실행

Python 3.10 이상과 Tkinter가 필요합니다. Windows의 공식 Python 설치에는 Tk가 포함됩니다.

```powershell
python -m pip install requests
python lobby_manager.py
```

`requests` 없이도 수동 조합 입력과 로컬 데이터 분석을 사용할 수 있습니다.

## 배치

- Windows에서 `클라이언트 따라가기`를 켜면 `LeagueClientUx.exe`의 표시된 창과 해당 모니터의 작업 영역을 읽어 배치합니다.
- 클라이언트를 이동하거나 크기를 바꾸면 약 0.8초 간격으로 패널 위치를 맞춥니다. 게임 창을 이동·크기 변경하거나 픽을 대신 확정하지 않습니다.
- 1280×720 클라이언트를 기준으로 양옆 224px, 아래 250px의 U자형 프레임을 만듭니다. 전체 크기는 1728×970px이며, 하단은 양쪽 패널의 바깥 가장자리와 정확히 맞닿습니다. 넓은 모니터에서도 화면 전체로 늘어나지 않습니다.
- 클라이언트 양옆에 각각 최소 170px, 아래에 240px 이상이 필요합니다. 좁은 쪽에 맞춰 양옆 너비를 동일하게 줄이고, 공간이 부족하면 팀 패널을 숨기고 안내합니다. 클라이언트를 줄이거나 옮긴 뒤 `기본 배치`를 누르세요.
- 클라이언트가 없으면 가능한 화면에서 중앙에 1280×720 공간을 비웁니다. 작은 모니터에서는 같은 16:9 비율로 줄입니다. 제목을 끌면 자동 따라가기가 꺼지고 Helper의 세 패널이 함께 움직입니다.
- Windows 하단 창은 기본 제목 표시줄 대신 어두운 전용 헤더를 사용합니다. 오른쪽 `—`로 최소화, `✕`로 종료하며 작업 표시줄에서 복원할 수 있습니다.
- 좌우 다섯 슬롯은 패널 높이에 맞춰 펼쳐집니다. 편집창을 열어 공간이 부족할 때만 스크롤바를 표시합니다.
- 다른 탭을 열거나 Helper를 최소화하면 팀 패널이 숨겨집니다. 팀 패널은 항상 위에 고정되지 않습니다.
- Windows 이외의 환경에서는 수동 배치를 사용합니다.

## 사용

1. 하단에서 내 라인을 선택합니다. `연결`로 LCU를 연결하거나 좌우 슬롯의 챔피언 이름을 눌러 직접 입력합니다.
2. 슬롯에서 챔피언과 라인을 선택하고 `적용`을 누릅니다. `라인 고정`은 기존의 수동 라인 유지 기능이고, `분석 제외`는 해당 슬롯을 추천 계산에서 제외합니다.
3. 하단 상위 후보를 선택하면 양옆에 그 후보 기준의 관계별 승률이 표시됩니다. 자기 슬롯과 제외된 슬롯은 관계 계산에서 빠집니다.
4. `전체 순위 / 필터`에서 상위 20개와 매치업별 근거를 확인합니다. 필터는 **최소 게임 수 또는 픽률** 조건이며 Enter 또는 `적용`으로 갱신합니다.
5. `더 보기`에서 스냅샷, 수동 불러오기, 선택 후보 제외, 초기화를 사용할 수 있습니다.

추천 점수는 시너지·상성 점수의 합이며 승리 확률이 아닙니다. `%`는 관계별 승률에만 사용합니다.
누락된 관계는 `자료 없음`으로 표시하고, 근거가 완전하지 않은 후보에는 `신뢰도 높음`이나 `올카운터`를 붙이지 않습니다.
후보 선택은 자동 갱신 후에도 해당 후보가 추천 목록에 남아 있으면 유지됩니다.

## 구조

- `draft_companion.py`: Hextech 테마, 슬롯 편집, 추천 카드와 팀 패널, 창 생명주기.
- `companion_layout.py`: 겹치지 않는 배치 계산, Windows 창 감지 및 DPI 초기화, 밴픽 변경 시그니처.
- `lobby_manager.py`: 기존 추천 엔진과 LCU 연동. JSON 캐시를 먼저 로드한 뒤 Tk 객체를 메인 스레드에서 생성해 초기화 경합과 종료 시 Tk 객체 정리 오류를 방지합니다.

새 런타임 의존성이나 웹뷰를 추가하지 않습니다. 기본 시스템 글꼴을 사용합니다.

## 공식 한글 챔피언 이름

화면의 챔피언 이름은 [라이엇 Data Dragon 16.18.1 한국어 데이터](https://ddragon.leagueoflegends.com/cdn/16.18.1/data/ko_KR/champion.json)의 173종을 기준으로 합니다.
`champion_aliases.json`에서 각 챔피언의 첫 한글 항목이 공식 표시 이름이며, 띄어쓰기도 원문을 유지합니다.
추천 카드·팀 패널·밴·상세 표·듀오·자동완성에는 한글 이름을 표시하고, 검색에는 기존 영문 이름과 별칭도 사용할 수 있습니다.
데이터 조회와 추천 선택에 사용하는 내부 식별자는 표시 이름과 분리합니다.

목록 갱신은 `python generate_aliases.py`로 수행합니다. 공식 영문·한글 데이터를 HTTPS로 조회하며 기존 검색 별칭은 보존합니다.
목록에 새 챔피언이 추가되어도 별도 통계 데이터가 없는 챔피언은 추천되지 않습니다.

## 검증

```powershell
python -m pip install pytest requests
$env:REQUIRE_TK = "1"
python -m pytest test_companion.py
python -m pytest
```

레이아웃 경계·음수 모니터 좌표·클라이언트와의 비겹침, 밴 전용 변경, 누락 자료,
추천 선택과 좌우 갱신, 동기식 GUI 초기화를 검증합니다.
실제 LoL 세션, 서로 다른 DPI의 다중 모니터 이동은 별도의 현장 확인이 필요합니다.
